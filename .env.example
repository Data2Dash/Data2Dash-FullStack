GROQ_API_KEY=""

# ─── Database ──────────────────────────────────────────────────────────────
# SQLite (default for local dev — no setup required):
DATABASE_URL="sqlite:///./data2dash.db"
# PostgreSQL (recommended for production):
# DATABASE_URL="postgresql://user:password@localhost:5432/data2dash"
YOUTUBE_API_KEY=""

# ─── Auth ─────────────────────────────────────────────────────────────────
# JWT – change this to a long random string in production
JWT_SECRET_KEY=""

# Google OAuth – get these from https://console.cloud.google.com/
# Create project → APIs & Services → Credentials → OAuth 2.0 Client ID
# Authorized redirect URI: http://localhost:8000/auth/google/callback
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
GOOGLE_REDIRECT_URI=""
FRONTEND_URL=""